Please write a C# data object class named "LJCPerson".
Use straightforward, common coding techniques and not highly optimized or
"tricky" code.

Use an indent of 2 spaces.
The first brace for methods, etc. starts on a new line.
Do not use "this." for accessing members except where necessary or to avoid
confusion.
Wrap lines that exceed 80 characters where possible.
Do not automatically wrap all arguments or parameters.
Wrap code statements before ",", "&&" or "||" where possible.

The data object class will have the following values:
ID long (database generated primary key),
ParentID long (foreign key),
Name string(60),
Description string(100).

Default Constructor
Create the default constructor.

Parameterized Constructor LJCPerson("long id, long parentID, string name, string
description = null")
Initialize the ID, ParentID, Name and Description properties.
Trim all string properties when the properties are set.

Copy Constructor LJCPerson(LJCPerson person)
Creates a deep copy of the provided person data object.
Return the new object.

Keep track of the original property values in a class called "OriginalValues".

Public Method Clone()
Use the Copy Constructor to create a deep copy of the data object.

Public Method SetOriginalState()
Sets the original values from the backing fields and clears the changed
properties.

Public Property ChangedProperties
Contains property names that have changed.
Remove a property name from "ChangedProperties" if the property is set to its
original value.
It is acceptable for the "ChangedProperties" property to be modifiable by the
calling program.

Use the [Required], [StringLength] and [Column] attributes where appropriate.

Create the members in this order using #region preceded with a new line:
Do not precede #endregion with an extra new line.
Static Methods:
Constructors:
Data Object Methods: Clone(), SetOriginalState()
Public Methods: 
Private Methods: TrackChanges()
Public Properties: ID, ParentID, Name, Description, ChangedProperties
Private Properties:
Public Fields:
Private Fields
Private Classes.

Return the code as files not as JSON.


Create an external XML file for this class with XML comments for public members
named "Doc/LJCPerson.xml".
Wrap lines that exceed 80 characters.

Add the <include> XML comments to the public class members to reference the
external member XML comments using the template:
/// <include path="members/ID" file="Doc/Person.xml"/>

Use this sample template for the external file:
<?xml version="1.0"?>
<members>
  <!-- Static Methods -->

  <!-- Class -->
  <LJCPerson>
  </LJCPerson>

  <!-- Constructor Methods -->
  <DefaultCtor>
  </DefaultCtor>
  <ParameterizedCtor>
  </ParameterizedCtor>
  <CopyCtor>
  </CopyCtor>

  <!-- Data Object Methods -->
  <Clone>
  </Clone>
  <SetOriginalState>
  </SetOriginalState>

  <!-- Methods -->

  <!-- Properties -->
  <ID>
  </ID>
  <Name>
  </Name>
  <Description>
  </Description>
  <ChangedProperties>
  </ChangedProperties>
</members>
